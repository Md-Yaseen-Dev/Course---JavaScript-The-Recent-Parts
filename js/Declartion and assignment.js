// Destructing all about assignments

function classroom(){

    return["1", "2","3"]
}

var tmp = classroom();

var firstclass,secondclass,thirdclass;

firstclass =tmp[0];
secondclass =tmp[1];
thirdclass =tmp[2];

console.log(secondclass);